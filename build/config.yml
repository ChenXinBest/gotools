# This file contains the configuration for this project.
version: '3'

info:
  companyName: "Xin Chen"
  productName: "GoTools"
  productIdentifier: "com.xinchen.gotools"
  description: "A collection of useful tools for developers"
  copyright: "(c) 2025, Xin Chen"
  comments: "GoTools - Developer Tools"
  version: "{{.Version}}"

dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .DS_Store
      - .gitignore
      - .gitkeep
      - "*_test.go"
    watched_extension:
      - "*.go"
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task common:dev:frontend
      type: background
    - cmd: wails3 task run
      type: primary
